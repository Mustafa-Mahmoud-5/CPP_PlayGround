#include <iostream>

using namespace std;


int main () {
  // extract two inputs, calc their sum, insert them to cout to output the stream to console
  int x,y;
  cin >> x >> y;
  cout << "Sum: " << x+y << '\n';
  cout << "Hello\nWelcome to console.";
  return 0;
}